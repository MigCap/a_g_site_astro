---
import Rights from "@/components/Rights/Rights.astro";
import Instagram from "@/components/icons/Instagram.astro";
---

<div class="contact_container">
  <p style={{ color: "var(--black)", fontWeight: "bold" }}>Studio</p>
  <p class="contact_mail">contact@alejandroguijarro.com</p>
  <a
    class="contact_social_link"
    href="https://www.instagram.com/alejandroguijarrostudio/"
    target="_blank"
    rel="noopener noreferrer"
    aria-label="Go to Alejandro Guijarro Instagram profile"
  >
    <Instagram />
    <p>Instagram</p>
  </a>

  <div class="contact_rights">
      <Rights />
  </div>
</div>

<style>
  .contact_container {
    max-height: calc(100vh - var(--nav-height));
    text-align: center;
    padding-top: 2rem;
    color: var(--grey);

    p {
        padding: 0;
    }

    a {
        font-size: 0.2rem;
        color: var(--grey);
        padding: 0.5rem 0;
        :hover {
          transition: var(--transition);
          color: ${colors.black};
        }

        svg {
            width: 0.7rem;
            color: var(--grey);
            :hover {
              transition: var(--transition);
              color: ${colors.black};
            }
        }
        p {
            color: var(--grey);
            font-size: 0.7rem;
            :hover {
              transition: var(--transition);
              color: ${colors.black};
            }
        }
    }
  }

  .contact_social_link {
    padding-bottom: 2%;

 
    :hover {
      transition: var(--transition);
      p {
        color: var(--black);
      }
      svg {
        color: var(--black);
      }
    }

    svg {
      color: var(--grey);
    }

    p {
      color: var(--grey);
    }
  }

  .contact_rights {
    grid-area: rights;
    grid-column: 1 / span 3;
    grid-row: 2;
    padding-top: 6rem;
    justify-self: center;
    font-size: .8rem;

    p {
      color: var(--grey);
      padding: 0 5px;
      a {
        font-size: 0.7rem;
        color: var(--grey);
        :hover {
          transition: var(--transition);
          color: var(--black);
        }
      }
    }
  }
</style>
